import * as core from '../../../core';

const channelPrivacyResolver = {
  PUBLIC: core.channels.ChannelPrivacy.Public,
  PRIVATE: core.channels.ChannelPrivacy.Private,
};

export default channelPrivacyResolver;
