import * as core from '../../../core';

const channelTypeResolver = {
  NAMED: core.channels.ChannelType.Named,
  DIRECT_MESSAGES: core.channels.ChannelType.DirectMessages,
};

export default channelTypeResolver;
